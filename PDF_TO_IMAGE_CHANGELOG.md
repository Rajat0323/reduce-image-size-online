# PDF to Image only — changelog

## Done

1. **Homepage UI shell kept** (studio look you approved)
2. **All blogs removed** — markdown deleted; `/blog/*` and `/articles/*` 301 → `/`
3. **Homepage marketing content removed** — only brand + tool (ready for your human copy later)
4. **Site is PDF → image only** — PNG / JPG / WEBP export, page range, ZIP download
5. Old image tools redirect to `/`
6. Sitemap: `/`, `/about`, `/contact`, `/privacy-policy`, `/terms` only

## Homepage content

Left intentionally empty below the tool so you can add human-written copy next.
